import { DataKey, NullableCoordinate } from 'recharts/types/util/types';
import { BaseValue } from 'recharts/types/cartesian/Area';
import { RechartsRootState } from 'recharts/types/state/store';
import { AxisId } from 'recharts/types/state/cartesianAxisSlice';
import { StackId } from 'recharts/types/util/ChartUtils';
import { ChartData } from 'recharts/types/state/chartDataSlice';
import { NullablePoint } from 'recharts/types/shape/Curve';
export interface AreaPointItem extends NullablePoint {
    x: number | null;
    y: number | null;
    value?: number | number[];
    payload?: any;
}
export type AreaSettings = {
    connectNulls: boolean;
    baseValue: BaseValue | undefined;
    dataKey: DataKey<any>;
    stackId: StackId | undefined;
    data: ChartData | undefined;
};
export type ComputedArea = {
    points: ReadonlyArray<AreaPointItem>;
    baseLine: number | NullableCoordinate[];
    isRange: boolean;
};
export declare const selectArea: (state: RechartsRootState, xAxisId: AxisId, yAxisId: AxisId, isPanorama: boolean, areaSettings: AreaSettings) => ComputedArea | undefined;
